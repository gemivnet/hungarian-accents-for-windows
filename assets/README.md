# Icon

Drop a Windows `.ico` file here named `icon.ico` and the build pipeline will
embed it into the compiled `.exe`. If absent, Ahk2Exe falls back to AutoHotkey's
default icon.

Recommended: a 256×256 ICO with multiple embedded sizes (16, 32, 48, 256).
